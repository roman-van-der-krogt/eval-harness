[project]
name = "eval-harness"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "openai>=2.8.1",
]

[dependency-groups]
dev = [
    "pytest>=9.0.1",
]
